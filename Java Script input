const readline = require('readline');
 //콘솔에서 입력받기 위한 코드
const rl = readline.createInterface({ //r1 -> readline모듈로 만든 인터페이스 객체
    input: process.stdin,
    output: process.stdout
});


rl.question(attempts + '번째 시도 : ', (input) =>{
  .
  .
  .
}
//콘솔창에서 입력받기 위해 위의 코드를 사용했음
